apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    cluster.x-k8s.io/v1beta1: v1beta1
    cluster.x-k8s.io/v1beta2: v1beta2
    clusterctl.cluster.x-k8s.io: ""
  name: begetmachinetemplates.infrastructure.cluster.x-k8s.io
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ $.Release.Name }}-{{ .Values.appSpec.applications.providerBegetControllerManager.volumes.webhook.volume.name }}
spec:
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          name: {{ $.Release.Name }}-{{ .Values.appSpec.applications.providerBegetControllerManager.name }}-webhook
          namespace: {{ $.Release.Namespace }}
          path: /convert
      conversionReviewVersions:
      - v1
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: BegetMachineTemplate
    listKind: BegetMachineTemplateList
    plural: begetmachinetemplates
    singular: begetmachinetemplate
  scope: Namespaced
  versions:
  - name: v1beta2
    schema:
      openAPIV3Schema:
        description: BegetMachineTemplate is the Schema for the begetmachinetemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              template:
                properties:
                  metadata:
                    description: Standard object's metadata.
                    minProperties: 1
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: BegetMachineSpec defines the desired state of BegetMachine.
                    properties:
                      configuration:
                        properties:
                          cpuCount:
                            format: int32
                            maximum: 16
                            minimum: 1
                            type: integer
                          diskSize:
                            format: int32
                            type: integer
                          memory:
                            format: int32
                            type: integer
                        required:
                        - cpuCount
                        - diskSize
                        - memory
                        type: object
                      image:
                        type: string
                      managedBy:
                        default: customer
                        enum:
                        - customer
                        - system
                        type: string
                      networkTag:
                        default: vps
                        type: string
                      providerID:
                        type: string
                      serverName:
                        type: string
                      sshKeyIds:
                        items:
                          format: int64
                          type: integer
                        type: array
                      usePrivateNetwork:
                        default: true
                        type: boolean
                    required:
                    - configuration
                    - image
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: BegetMachineTemplate is the Schema for the begetmachinetemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              template:
                properties:
                  metadata:
                    description: Standard object's metadata.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: BegetMachineSpec defines the desired state of BegetMachine.
                    properties:
                      configuration:
                        properties:
                          cpuCount:
                            format: int32
                            maximum: 16
                            minimum: 1
                            type: integer
                          diskSize:
                            format: int32
                            type: integer
                          memory:
                            format: int32
                            type: integer
                        required:
                        - cpuCount
                        - diskSize
                        - memory
                        type: object
                      image:
                        type: string
                      managedBy:
                        default: customer
                        enum:
                        - customer
                        - system
                        type: string
                      networkTag:
                        default: vps
                        type: string
                      providerID:
                        type: string
                      serverName:
                        type: string
                      usePrivateNetwork:
                        default: true
                        type: boolean
                    required:
                    - configuration
                    - image
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: false